# =============================================================================
# whitelist.conf — Accounts exempt from user activity alerts
# These are system services that legitimately log in at odd hours
# =============================================================================

# =================== module 2 : user activity thresholds ===================   
WHITELISTED_USERS="lightdm postgres wtmpdb"

# =================== module 3 : process and network ===================   
WHITELISTED_PROCESSES=("
/usr/bin/msfrpcd
/usr/bin/nmap
")
